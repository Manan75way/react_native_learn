import { View, Text, Button } from "react-native";
import { Link } from "expo-router";

const Register = () => {
  return (
    <View>
      <Text>Register </Text>
    </View>
  );
};

export default Register;
